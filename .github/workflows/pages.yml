name: Disabled custom Pages workflow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - name: Branch-based Pages only
        run: echo "This repository uses GitHub Pages deployed from the main branch root."
